server {
    listen 80;
    listen [::]:80;
    server_name $DOMAIN;

    # Used by certbot for the HTTP-01 (webroot) challenge.
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 503;
    }
}
